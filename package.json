{
  "name": "reply-better-ai",
  "version": "1.6.2",
  "description": "Improve and reply to messages with AI — free on-device, local (Ollama/LM Studio), Groq, or OpenRouter. Chrome + Firefox.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "package": "node build.mjs --package",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "openai": "^4.73.0",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
